export interface Nome {
 
     primeiro_nome: string,
     sobrenome: string,
     nome_meio: string
  

  // nomeCompleto(){
  //   return `${this.nome} ${this.complemento} ${this.sobrenome}`
  // }
}